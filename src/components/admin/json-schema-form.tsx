/**
 * ra-json-schema-form: JSON Schema driven forms.
 *
 * Re-exports the JsonSchemaForm component from ra-json-schema-form.
 * Uses @rjsf (React JSON Schema Form) under the hood (Apache-2.0 licensed).
 *
 * Example usage:
 *   import { JsonSchemaForm } from "@/components/admin/json-schema-form";
 *   <JsonSchemaForm schema={myJsonSchema} />
 */
export { JsonSchemaForm } from "@react-admin/ra-json-schema-form";
