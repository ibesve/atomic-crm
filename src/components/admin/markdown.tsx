/**
 * ra-markdown: Markdown input and field components.
 *
 * Re-exports MarkdownInput and MarkdownField from ra-markdown.
 * Uses @toast-ui/editor under the hood (MIT licensed).
 *
 * Example usage:
 *   import { MarkdownInput, MarkdownField } from "@/components/admin/markdown";
 *   // In a form:
 *   <MarkdownInput source="description" />
 *   // In a show view:
 *   <MarkdownField source="description" />
 */
export { MarkdownInput, MarkdownField } from "@react-admin/ra-markdown";
